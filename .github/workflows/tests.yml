name: Contract tests
on:
  push:
    paths: ["contracts/**", "test/**", "foundry.toml"]
  workflow_dispatch:
jobs:
  forge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: foundry-rs/foundry-toolchain@v1
      - run: forge test -vv
